{
  "name": "@codesociety-net/typescript",
  "version": "1.0.0",
  "description": "Design pattern implementations in TypeScript — verified, tested, benchmarked",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "benchmark": "tsx benchmarks/run.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "tsx": "^4.21.0",
    "tinybench": "^3.0.0"
  },
  "license": "MIT"
}
